[tool.ruff]
target-version = "py311"
line-length = 88
exclude = ["alembic"]

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.black]
target-version = ["py311"]
line-length = 88
extend-exclude = "/alembic/"

[tool.isort]
profile = "black"
skip_glob = ["**/alembic/**"]
